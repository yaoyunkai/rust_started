[package]
name = "part16_concurrency"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.44"
hello_world = { path = "../hello_world" }
#serde = "1.0.228"


[lints.rust]
# 忽略未使用的方法、结构体、枚举等
dead_code = "allow"
# 忽略未使用的变量
unused_variables = "allow"

